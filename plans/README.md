# Implementation Plans

Generated by the improve skill (deep audit) on 2026-06-13, against commit
`41d69c7` (main, v0.3.0.0). Execute in the order below unless dependencies
say otherwise. Each executor: read the plan fully before starting, honor its
STOP conditions, and update your row when done.

Repo verification battery (used by every plan):
`npm run lint && npx tsc --noEmit && npm run build && npm test`
(the `typecheck` script is added by plan 001; until then use `npx tsc --noEmit`).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | CI verification gates (typecheck/lint/build + workflow permissions) | P1 | S | — | DONE (approved; worktree `worktree-agent-a077cc69ef875f49b`) |
| 002 | Next.js 16.2.9 security patch + lockfile resync | P1 | S | 001 (soft) | DONE (approved; worktree `worktree-agent-abeee518630a52478`; 4 HIGH advisories cleared, 3 pre-existing dev/postcss moderates remain) |
| 003 | AI-context truth pass (152M fix, build-time last-updated, copy-button res.ok) | P2 | S | — | DONE (approved; worktree `worktree-agent-a249a5d8453d0c0fe`) |
| 004 | Orphan CSS on media-kit/context CTAs + dead exports/assets/tsbuildinfo | P2 | S | — | DONE (approved; worktree `worktree-agent-a5f745674db1f0ac6`; tsbuildinfo step was a no-op, see below) |
| 005 | Build-time YouTube RSS refresh of the video reel | P2 | M | 001 (soft) | DONE (approved; commits included in plan 008's branch `worktree-agent-aa09439be73186b30`) |
| 006 | Test hardening (reduced-motion branches, load-context, llm.txt route, stub dedup) | P3 | M | — | DONE (approved; worktree `worktree-agent-a218098734d7edf1c`; 63 to 72 tests) |
| 007 | Idle motion cost (Lenis rAF visibility pause, offscreen marquee pause) | P3 | M | — | DONE (approved; worktree `worktree-agent-a2bc4c020e4491e04`) |
| 008 | VideoObject structured data for the reel | P3 | S-M | 005 (soft) | DONE (approved; worktree `worktree-agent-aa09439be73186b30`, stacked on 005) |
| 009 | Hero atmosphere image — taller Seoul dusk crop | P3 | S | — | TODO |
| 010 | Media Kit cinematic layer (homepage parity) | P2 | M | — | DONE (merged to main, 2026-06-27) |
| 011 | Icon set migration (Lucide → Phosphor) | P3 | S-M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 first: it is the verification baseline; every later plan's done
  criteria lean on lint+typecheck+build+test running in CI.
- 002 is urgent independent of 001; if run before 001, use `npx tsc --noEmit`
  directly.
- 008 wants ISO publish dates; 005 provides them automatically, but 008
  carries a pinned fallback table so it can run standalone.
- 003 and 006 both touch context-related tests: if 003 landed,
  CopyContextButton tests exist already — 006 must not duplicate them.
- 006 and 007 both may create reduced-motion/marquee tests; whichever runs
  second extends rather than duplicates.

## Findings considered and NOT planned (so nobody re-audits them)

- **NEWAKE demo font licensing** (`src/fonts/NewakeFont-Demo.otf`, no license
  file in repo): real finding, HIGH potential impact, but it needs the
  operator's answer (license may exist with the brandbook designer). Operator
  decision pending; do not "fix" by swapping fonts without instruction.
- **CSP header**: legitimate hardening but needs a Next-App-Router-compatible
  policy (inline flight scripts); deferred until someone owns the testing.
- **Hero atmosphere image re-export** (TODOS P3): needs image regeneration
  tooling; operator task, see plan 007 maintenance notes.
- **CSS route-splitting**: ~9KB gzipped total stylesheet; not worth M effort.
- **next/dynamic section code-splitting**: sections are mostly server
  components; expected gain too small to justify layout-shift risk.
- **Self-hosting YouTube thumbnails**: Vercel image optimizer already proxies
  and caches i.ytimg.com.
- **Unifying the 4 document-level mousemove hooks**: measure first; no
  evidence of jank yet (investigate-tier).
- **Bundle measurement (motion/lucide tree-shaking)**: cheap to do during any
  perf session; no action specified until measured.
- **Full hook test suite + Playwright visual layer**: L-effort; operator does
  interactive headless QA via gstack; revisit if regressions start escaping.
- **prettier / husky / editorconfig**: solo repo with an agent-driven
  workflow; formatting drift has not been an observed cost.
- **React/TypeScript/ESLint/lucide major bumps**: deliberate holds; revisit
  on their own schedule, not as debt.
- **Marquee implementation consolidation** (TypeMarquee vs SocialProof
  inline): the two render different content shapes; consolidation cost >
  divergence cost today.

## Rejected as false positives (audited, disproven)

- "render-markdown uses match.index as React key — DOM reuse bug": keys are
  unique within each render and the list is rebuilt per render; no bug.
- "NEWAKE font lacks preload": `next/font/local` injects preload links
  automatically.
- "tsconfig.tsbuildinfo committed to git": disproven during plan 004
  execution; the file was never tracked (the .gitignore rule works). The
  plan step was correctly skipped as a no-op.

## Execution record (2026-06-13)

All 8 plans executed by sonnet subagents in isolated worktrees and approved
after review (done criteria re-run, scope verified, diffs read, tests audited).
Branches awaiting landing, suggested merge order into a feature branch:
001 -> 002 -> 003 -> 004 -> 006 -> 007 -> 008 (008's branch contains 005).
Expected conflicts: package.json scripts block (001 adds typecheck, 005 adds
prebuild) — keep both lines; test/videos.test.ts is touched only by 005.
After merging, run the full battery and re-run the refresh script once.
